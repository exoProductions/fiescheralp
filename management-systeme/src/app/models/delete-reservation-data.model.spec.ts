import { DeleteReservationData } from './delete-reservation-data.model';

describe('DeleteReservationData', () => {
  it('should create an instance', () => {
    expect(new DeleteReservationData()).toBeTruthy();
  });
});
